<?xml version="1.0" encoding="utf-8"?>
<!-- 本机播放 / 图片预览页：黑底沉浸，点空白关闭 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/localRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF000000">

    <VideoView
        android:id="@+id/videoView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:visibility="gone"
        tools:visibility="gone" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:contentDescription="图片预览"
        android:scaleType="matrix"
        android:visibility="gone"
        tools:visibility="gone" />

    <!-- 音频时显示的信息区 -->
    <LinearLayout
        android:id="@+id/audioPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingHorizontal="32dp"
        android:visibility="gone"
        tools:visibility="gone">

        <ImageView
            android:id="@+id/localArt"
            android:layout_width="220dp"
            android:layout_height="220dp"
            android:background="@drawable/bg_art_round_lg"
            android:clipToOutline="true"
            android:contentDescription="专辑封面"
            android:outlineProvider="background"
            android:scaleType="centerCrop"
            android:visibility="gone"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/localAudioTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:ellipsize="marquee"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/player_text"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/localAudioMeta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/player_subtext"
            android:textSize="13sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:text="本机播放中（点屏幕任意处关闭）"
            android:textColor="@color/player_faint"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- 顶部标题（视频/图片时显示） -->
    <TextView
        android:id="@+id/tvLocalTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="18dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#FFFFFFFF"
        android:textSize="14sp" />

    <ProgressBar
        android:id="@+id/localLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        tools:visibility="visible" />
</FrameLayout>
